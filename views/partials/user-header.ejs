<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <link rel="stylesheet" href="/style/output.css" />
        <script src="https://use.fontawesome.com/0aee473986.js"></script>
    </head>
    <body>
        <header></header>
        <aside
            class="fixed left-0 top-0 z-40 h-screen w-64 -translate-x-full transition-transform sm:translate-x-0"
        >
            <nav
                class="flex h-full flex-col overflow-y-auto bg-slate-700 px-3 py-4 text-center text-white"
            >
                <div class="my-4">
                    <a
                        href="/dashboard"
                        class="bord mb-10 flex items-center rounded-sm border-b-2 border-slate-500 py-8 pl-2.5"
                    >
                        <img
                            src="/img/pulse-white.png"
                            class="mr-3 h-12 rounded-full bg-sky-400 text-white"
                            alt="pulse Logo"
                        />
                        <p
                            class="self-center font-sans text-3xl font-semibold text-white"
                        >
                            Pulse
                        </p>
                    </a>
                    <ul class="space-y-2 font-medium">
                        <li>
                            <a
                                class="flex items-center rounded-lg p-2 font-sans text-white hover:bg-sky-400"
                                href="/dashboard/user"
                            >
                                <p class="ml-3">Dashboard</p>
                            </a>
                        </li>
                    </ul>
                </div>
                <div class="mt-auto">
                    <div class="flex items-center p-2 font-sans text-white capitalize">
                        <img
                            src="/img/user-white.png"
                            class="mr-3 h-5"
                            alt="pulse Logo"
                        />
                        <p><%= user %></p>
                    </div>
                </div>
                <form action="/logout" method="POST">
                    <input
                        class="rounded w-full bg-sky-400 px-10 py-2 font-sans font-bold text-white transition-colors hover:cursor-pointer hover:bg-sky-500 active:bg-sky-600"
                        type="submit"
                        name="doit"
                        value="Logout"
                    />
                </form>
            </nav>
        </aside>
        <main class="h-screen bg-gray-100 p-4 sm:ml-64">
